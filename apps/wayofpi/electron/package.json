{
  "name": "wayofpi-technical-electron",
  "productName": "Way of Pi — Technical IDE",
  "version": "0.0.1",
  "description": "Technical IDE — standalone Electron app for Way of Pi",
  "main": "electron/main.js",
  "scripts": {
    "electron": "electron .",
    "dev": "concurrently -k -n server,ui -c blue,green -p 'server: {name} ' --prefix-colors blue,green \"bun run dev:electron\" \"bun run dev:ui\"",
    "dev:electron": "electron .",
    "dev:ui": "bun run ../../technicalIDE/vite.config.ts",
    "server": "bun run ../server/index.ts",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "bun": "^1.0.0"
  }
}
