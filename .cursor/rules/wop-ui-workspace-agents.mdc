---
description: Way of Pi UI — workspace agents MUST align with Pi runtime; catalog vs Pi-owned turns
globs: apps/wayofpi-ui/**/*
alwaysApply: false
---

# Way of Pi UI: workspace agents

Use this when changing **`apps/wayofpi-ui/`** agent discovery, **My AI Team**, **workspace agent** selection, chat system prompts, or anything that should stay aligned with **Pi `agent-team` / `system-select`** semantics.

## CRITICAL — Pi owns agent behavior (tools + extensions)

- Way of Pi **does not** ship or grow its **own** agent/orchestration **system** in Bun — it **uses Pi’s**. The web UI is a **client + host** around **`WOP_PI_BINARY`** (see **`.cursor/rules/wop-ui-pi-backend-parity.mdc`** § “Do not build or own”).
- **Agent frontmatter `tools:`**, **skills**, and **extension-registered** capabilities are meaningful **only** when the **Pi coding-agent process** runs the turn. **Merging** an agent **`.md` body** into the interim Bun/Ollama path **does not** enable Pi tools (**persona text only**). With **`WOP_CHAT_ENGINE=pi`** or **`auto`** and a resolved **`pi`** CLI, **`runPiChatTurn`** runs **`pi --mode json`** for **every** persona (orchestrator + **`.md`** agents) so built-ins and extensions match the TUI (**`piDrivesChat`**).
- **Orchestrator** and **every specialist** must converge on the **same** target: **headless Pi** behind **`WOP_PI_BINARY`**, not parallel Bun tool loops.
- Do **not** document or ship UI that implies **`dispatch_agent`**, **`team_member_*`**, or extension tools ran **unless** the server proves a Pi subprocess handled it.

## Canonical sources on disk

- **Agent definitions:** Markdown + YAML frontmatter (`name`, `description`, `tools`, `skills`, body), same as **[docs/AGENTS.md](docs/AGENTS.md)**.
- **Primary playground location:** **`.pi/agents/`** (plus **`agents/`**, **`.claude/agents/`**, **`.cursor/agents/`** per Pi-style scan).
- **Rosters:** **`.pi/agents/teams.yaml`** — team names → agent `name` lists; first file win for duplicate `name` across scan roots (mirror **`extensions/agent-team.ts`** / server **`server/agents.ts`**).

## Server responsibilities (do not bypass)

- **Catalog:** **`GET /api/agents`** — implemented in **`apps/wayofpi-ui/server/agents.ts`** (`loadWorkspaceAgents`). Keep scan order and de-dupe rules identical to Pi’s agent-team behavior unless product explicitly diverges (then document in **`docs/WOP_WORKSPACE_AGENTS_UI_PLAN.md`**).
- **Active persona:** WebSocket message (e.g. `agent` / `set_agent` handling in **`apps/wayofpi-ui/server/index.ts`**) resolves the chosen **`name`** with **`getAgentBodyByName`**, updates **`cachedAgentBody`**, and reapplies **`applyLeadSystem`** — same agent file the TUI would load for a single-session persona switch. **Until Pi owns chat**, this is **prompt merge only**, not Pi session switch.

## Simple vs technical UI

- **One source of truth in the client:** **`useAgents()`** (`/api/agents`) and **`useWayOfPiSession`** agent state. **Simple** (**`SimpleTeamView`**, **`SimpleChatView`**) and **technical** (**`ChatPanel`**) must not fork separate discovery logic or different API paths.
- **Technical workspace grid:** When **View → Editor Layout** uses multiple **`WorkspacePane`** cells, file/buffer state is **per cell**; **explorer open** targets the **focused** cell (see **`docs/WOP_TECHNICAL_UI.md`**, **`TechnicalWorkspaceGrid.tsx`**). Agent/chat behavior is unchanged—still **`ChatPanel`** docked right/bottom.
- **Workspace agent** dropdown: populated from the same `agents[]` list; changing mode must not reset catalog behavior—only layout/chrome differs.

## 404 and “API not found”

- The SPA uses **relative** `fetch("/api/…")` (**`src/api/client.ts`**). **`/api/agents`** and **`/api/llm/models`** exist only on the **Way of Pi Bun server** (`WOP_SERVER_PORT`, default **3333**). **Vite dev** proxies **`/api`** and **`/ws`** to that server (**`vite.config.ts`**).
- If the user opens **only** static **`dist/`** (or any host without the API), the browser returns **404** — not an empty agent list. Fixes are **operational** (run the server) or **product** (documented in the workspace agents plan: health banner, base URL, or embed API). Do not “fix” 404 by silently faking agents in the client.

## Pi parity: catalog today vs Pi-owned turns (target)

- **Today (interim):** One connection streams via Bun to **Ollama/OpenRouter**; choosing an agent **merges** that agent’s markdown into the system prompt (**`server/session-prompts.ts`**). **`tools:`** documents intent; **execution** is **not** Pi unless the user uses **Pi TUI**.
- **Target:** **Every** turn (orchestrator + specialists) runs in **headless Pi** so **extensions**, **tools**, and **`dispatch_agent`** match the TUI — **`docs/WOP_PI_BACKEND_WIRING_PLAN.md`**, **`docs/WOP_MULTI_AGENT_WEBSOCKET.md`**, **`docs/WOP_WORKSPACE_AGENTS_UI_PLAN.md`**.

## Related docs

- Plan and phases: **`docs/WOP_WORKSPACE_AGENTS_UI_PLAN.md`**
- Agent definitions: **`docs/AGENTS.md`**, **`docs/AGENT_TEAMS.md`**
- Technical shell overview: **`docs/WOP_TECHNICAL_UI.md`**
- Modular docks (where agent panels live): **`.cursor/rules/wop-ui-modular-docks.mdc`**
